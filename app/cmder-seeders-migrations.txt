ver comandos
    npx sequelize-cli

// MIGRATION 
Estructura (tabla + columnas)

Create Table
npx sequelize-cli migration:generate --name CreateCategoryTable

Ver migraciones no ejecutadas al momento
npx sequelize-cli db:migrate:status

Ejecutar migracion
npx sequelize-cli db:migrate

Revertir última migración
npx sequelize-cli db:migrate:undo:all

// MODELOS

// FACTORY
npm i faker

// SEED
Query (insert)

Create Seeder
npx sequelize-cli seed:generate --name ...Seeder

Ejecutar Seeder
npx sequelize-cli db:seed:all

Revertir Seeder
npx sequelize-cli db:seed:undo:all




